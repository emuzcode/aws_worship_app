<% provide(:title, @user.name) %>

<div class="like-title text-center">
  <div class="user-name">
    <%= @user.name %>さんのお気に入り
  </div>
</div>
<ul>
  <% if @user.likes.any? %>
    <% @likes.each do |song| %>
      <li id="song_<%= song.id %>">
        <%= link_to song.name_en, song %>
      </li>
    <% end %>
  <% else %>
    お気に入りはまだ登録されていません
  <% end %>
</ul>